An example of a basic door client and server program.

## Building
Adjust the `BSD_SRC` Makefile variable to point to your previously donwloaded and patched sources.
After building with `make`, execute `server` and `client`.
